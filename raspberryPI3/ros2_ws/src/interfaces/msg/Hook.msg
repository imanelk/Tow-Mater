string type         # "detection" or "lock"
bool status false   # true for "lock" or "detect", false for "unlok" or "no detect", according to the type
float32 x -1.0
float32 y -1.0
float32 distance -1.0