bool left_lidar false   # true when there is a high intensity on the left
bool right_lidar false  # same as "left"
float32 left_min        # minimum distance with an obstacle on the left
float32 right_min       # same as "left"